services:
  queue-workers:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    restart: unless-stopped

  mini-bot:
    build:
      context: .
      dockerfile: Dockerfile.minibot
    env_file: .env
    restart: unless-stopped
